---
// src/components/section/AccordionHowWeWork.astro

const { lang = 'es' } = Astro.props;

const translations = {
	es: {
		heading: 'Cómo trabajamos',
		paragraph01:
			'Dentro del equipo tenemos roles bien definidos. Cada uno de nosotros aporta algo único: ',
		list: [
			'Creatividad (Dirección Creativa y relación con clientes)',
			'Precisión técnica (Arquitecto técnico)',
			'Sensibilidad estética (Diseñadora de interiores)',
			'Organización y coordinación de proyectos (Gestión logística)',
		],
		paragraph02:
			'Esto nos permite ofrecer proyectos integrales y armónicos que cumplen con las expectativas de nuestros clientes.',
	},
	en: {
		heading: 'How We Work',
		paragraph01:
			'Within the team, we each have clearly defined roles. Every one of us brings something unique to the table:',
		list: [
			'Creativity (Creative direction and client relations)',
			'Technical precision (Technical architect)',
			'Aesthetic sensitivity (Interior designer)',
			'Organization and project coordination (Logistics management)',
		],
		paragraph02:
			'This allows us to deliver cohesive, well-rounded projects that meet our clients’ expectations.',
	},
};

const t = translations[lang] ?? translations['es'];
---

<details open class="shadow-2xl">
	<summary
		class="bg-accent group radius border-accent relative mx-auto flex w-full cursor-pointer items-center justify-center border-b-2 p-2"
	>
		<h3 class="w-full text-center text-xl font-bold">{t.heading}</h3>
		<span class="absolute right-4 transition-transform group-open:rotate-180">▼</span>
	</summary>

	<div class="border-accent radius -mt-2 flex flex-col gap-4 border-2 p-6 text-left">
		<p>{t.paragraph01}</p>
		<ul class="text-secondary line-inside flex list-disc flex-col space-y-1 px-4 text-base">
			{t.list.map((list) => <li>{list}</li>)}
		</ul>
		<p>{t.paragraph02}</p>
	</div>
</details>
